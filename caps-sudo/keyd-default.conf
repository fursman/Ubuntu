# keyd config for caps-sudo.
# CapsLock stops being a lock key entirely and instead fires the toggle helper.
# Because it no longer drives xkb's lock state, the CapsLock LED is free for
# caps-sudo to use as the "armed" indicator.
[ids]
*

[main]
capslock = command(/usr/local/sbin/caps-sudo keypress)
