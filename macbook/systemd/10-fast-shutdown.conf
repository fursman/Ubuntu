# Bound how long a stopping unit may hang. Without this a single stuck service
# can leave the machine half-off in a bag, fans running, battery draining --
# which matters much more once the lid does a real shutdown rather than a
# suspend. Ubuntu's default is 90s.
[Manager]
DefaultTimeoutStopSec=10s
